# AI 검색 어시스턴트 개편 보고서

**날짜:** 2026-05-09  
**작업 범위:** 마이페이지 & 스퀘어 K AI 검색 기능 전면 재설계 + 버그 수정  
**관련 커밋:** `206e29b` (챗봇강화) → `b6f5435` (영상 검색 강화)

---

## 1. 배경 — 기존 방식의 문제

| 항목 | 기존 방식 | 문제 |
|---|---|---|
| 검색 범위 | 벡터 임베딩 유사도 기반 상위 **10개** 사전 필터링 | 관련 영상이 10개 안에 없으면 Gemini가 아예 못 봄 |
| Gemini 역할 | 일반 챗봇 (대화 응답) | 영상 ID 반환 방식이 불안정 |
| 한국어 처리 | 없음 | "요리를 찾아줘" → "요리를" 매칭 실패 |
| 카테고리 검색 | 없음 | "레시피 영상" 검색 시 `recipe` 카테고리 매칭 안 됨 |

---

## 2. 신규 설계 — 2단계 파이프라인

```
전체 영상 (N개)
    │
    ▼ [1단계] 클라이언트 키워드 필터  (FloatingChat.tsx)
    │  - 조사 제거 (은/는/이/가/을/를 등 ~20종)
    │  - 카테고리 동의어 매칭 (recipe → 요리/레시피/음식/쿠킹...)
    │  - 가중치 스코어링: 제목(+3) > 카테고리(+2) = 태그(+2) > 채널(+1) = 내용(+1)
    │  → 상위 50개 후보 선별
    │
    ▼ [2단계] Gemini 시맨틱 랭킹  (api/chat/route.ts)
       - 50개 후보를 컨텍스트로 전달
       - 검색 의도 기반 재정렬
       - 관련도 높은 순 최대 8개 ID 반환
       → 채팅창에 영상 카드로 노출
```

---

## 3. 수정 파일 상세

### 3-1. `components/chat/FloatingChat.tsx`

#### 추가된 상수 및 함수

**`CAT_SYNONYMS`** — 카테고리 동의어 맵
```ts
const CAT_SYNONYMS: Record<string, string[]> = {
  recipe:  ['요리', '레시피', '음식', '쿠킹', '조리', '밥', '반찬', ...],
  english: ['영어', '영문', '영어학습', '영어공부', ...],
  learning:['학습', '공부', '강의', '수업', '교육', ...],
  // ... 10개 카테고리 전체
}
```
→ 사용자가 "레시피" 검색 시 `recipe` 카테고리 영상도 매칭됨

**`stripKoreanParticles(word)`** — 한국어 조사 제거
```ts
function stripKoreanParticles(word: string): string {
  return word.replace(/(에서|에게|이라고|...|은|는|이|가|을|를|의|...)$/u, '')
}
```
→ "요리를" → "요리", "영어공부는" → "영어공부" 로 정규화

**`formatSavedDate(createdAt)`** — Firestore Timestamp → 'YYYY.MM.DD'

**`extractShortText(category, summary)`** — 카테고리별 핵심 텍스트 추출  
→ 저장된 요약 데이터에서 검색 대상 텍스트 생성 (카테고리별 필드 분기)

**`buildCandidates(summaries, query, limit=50)`** — 1단계 필터 핵심 함수
```
1. 전체 summaries를 최신순 정렬
2. 쿼리 토큰화 → stripKoreanParticles 적용 → 2자 미만 제거
3. 각 영상에 가중치 스코어 계산
4. score > 0 인 항목을 내림차순 정렬 → 최대 50개
5. 50개 미만이면 최신 항목으로 채움 (fill)
```

#### handleSend 수정

**기존:** 고정 후보 10개 → Gemini 전달  
**변경:** `buildCandidates(summaries, query, 50)` 호출 → 50개 → Gemini 전달

#### 후속 질문 맥락 유지 (버그 수정, 커밋 b6f5435)

**문제:** "아니 영상 목록을 달라고" 같은 후속 질문 시, 새 query로 `buildCandidates` 재호출 → 이전 AI 영상 후보가 교체되어 Gemini가 찾지 못함

**수정:** `lastCandidatesRef`에 직전 후보 목록 유지, 후속 메시지 시 병합
```ts
const lastCandidatesRef = useRef<SummaryMeta[]>([])

// handleSend 내부
const fresh = buildCandidates(summaries, query, 50)
let summaryMeta = fresh
if (messages.length > 0 && lastCandidatesRef.current.length > 0) {
  const freshIds = new Set(fresh.map(m => m.id))
  summaryMeta = [
    ...fresh,
    ...lastCandidatesRef.current.filter(m => !freshIds.has(m.id)),
  ].slice(0, 50)
}
lastCandidatesRef.current = summaryMeta
```
→ "AI 관련 영상 찾아줘" 다음 "목록 다시 보여줘" 해도 같은 후보로 응답

#### UI 변경

| 항목 | 변경 전 | 변경 후 |
|---|---|---|
| 플로팅 버튼 아이콘 | 💬 | 🔍 |
| 버튼 라벨 | AI 어시스턴트 | AI 맞춤검색 |
| 헤더 타이틀 | AI 어시스턴트 | AI 검색 어시스턴트 |
| 입력 플레이스홀더 | (기존 챗봇형) | 찾고 싶은 영상을 자연어로 검색... |

---

### 3-2. `app/api/chat/route.ts`

#### 구조 변경

**기존 구조:**
```
벡터 임베딩 검색 → 유사도 정렬 → 상위 10개 → Gemini 챗봇 응답
```

**변경 후 구조:**
```
클라이언트에서 50개 후보 수신 → 컨텍스트 리스트 빌드 → Gemini 시맨틱 랭킹 → [RELATED:id,...] 파싱
```

#### 제거된 코드
- `fetchEmbeddings()` — 벡터 임베딩 DB 조회 함수 전체 제거
- `cosineSim()` — 코사인 유사도 계산 제거
- `embeddingModel` — 임베딩용 Gemini 모델 인스턴스 제거
- 키워드 사전 스코어링 로직 제거

#### Gemini 호출 설정

```ts
generationConfig: {
  temperature: 0.1,       // 검색 결과는 결정론적으로
  maxOutputTokens: 512,   // 256 → 512 (ID 22개 반환 시 잘림 버그 수정)
  thinkingConfig: { thinkingBudget: 0 },  // 추론 비용 절감
}
```

#### 시스템 프롬프트 (요약)

```
당신은 AI 검색 도우미입니다.
아래 영상 후보 목록에서 관련도 높은 순으로 정렬하세요.
- 제목/카테고리/채널/태그/내용/저장일 종합 판단
- 응답 형식:
  줄1: 1~2문장 자연어 설명
  줄2: [RELATED:id1,id2,...] — 최대 8개
```

#### RELATED 태그 파싱 버그 수정 (커밋 b6f5435)

**문제:** 결과 영상이 많을 때(22개 등) Gemini 출력이 512토큰 초과로 `]` 없이 잘림  
→ 정규식 `/\[RELATED:[^\]]*\]/` 가 닫는 괄호 없으면 매칭 실패  
→ `[RELATED:id1,id2...]` 태그가 채팅창에 그대로 노출

**수정:**
```ts
// 기존: 닫는 ] 필수
rawText.match(/\[RELATED:([^\]]*)\]/)

// 변경: 닫는 ] 선택적 (잘린 경우도 파싱)
rawText.match(/\[RELATED:([^\]]*)\]?/)

// 스트립 정규식도 동일하게 수정
rawText.replace(/\[RELATED:[^\]]*\]?/g, '').trim()

// 최대 8개 강제 슬라이싱 추가
.slice(0, 8)
```

---

## 4. 검색 품질 비교

| 검색어 | 기존 | 개편 후 |
|---|---|---|
| "요리를 찾아줘" | "요리를" 포함 영상만 (조사 포함 매칭) | "요리" 정규화 후 레시피/음식/쿠킹까지 매칭 |
| "ai관련 최근 영상 5개" | 임베딩 top-10 제한으로 누락 가능 | 전체에서 AI 관련 스코어링 → Gemini 재정렬 |
| "중학교 수학영상" | 태그·제목 단순 포함 검색 | 카테고리 `learning` 동의어 + 제목 가중치 합산 |
| 후속질문 "목록 다시 보여줘" | 새 쿼리로 후보 교체 → 다른 결과 | 이전 후보 병합 → 동일 맥락 유지 |

---

## 5. 비용 / 성능

- Gemini 2.5 Flash, 50후보 × ~400자 ≈ **5,000토큰** → 건당 약 $0.0004
- `thinkingBudget: 0` 으로 추론 비용 완전 차단
- `temperature: 0.1` 으로 일관된 랭킹 결과 보장
- 벡터 임베딩 API 호출 **완전 제거** → 레이턴시 감소

---

## 6. 잔여 개선 포인트

- [ ] 카테고리 동의어(`CAT_SYNONYMS`) 영어 키워드 추가 (일본 런칭 고려)
- [ ] 검색 결과 0개일 때 "비슷한 검색어" 제안 기능
- [ ] 장기적으로 한국어 형태소 분석 라이브러리(konlp 등) 도입 검토
