# SSOKTUBE — 전체 기능 명세

> 최종 업데이트: 2026-05-05

---

## 1. 핵심 분석 기능

### 1-1. YouTube 영상 AI 요약
- YouTube URL(youtube.com/watch, youtu.be, playlist·si 파라미터 무시하고 정상 처리) 붙여넣기로 즉시 분석
- 자막 추출 → 카테고리 자동 분류 → Gemini 2.5 Flash 깊이 요약
- 자막 없는 영상: 영상 설명 + 댓글 기반 요약으로 폴백, 경고 배너 표시
- 30분 이상 긴 영상: 구간별 분석 탭 자동 활성화
- 같은 영상 재요청 시 DB 캐시 반환 (API 절약)

### 1-2. 카테고리별 요약 구조
| 카테고리 | 핵심 출력물 |
|---------|-----------|
| `recipe` 요리 | 재료 그룹별 분류, 단계별 조리법, 팁 |
| `english` 영어학습 | 핵심 표현, 단어장, 문법 패턴, 문화 맥락 |
| `learning` 학습 | 개념 정의, 핵심 포인트, 강사 예시 |
| `news` 뉴스/시사 | 헤드라인, 3줄 요약, 육하원칙, 시사점 |
| `selfdev` 자기계발 | 핵심 메시지, 실천 액션, 명언 |
| `travel` 여행 | 여행지, 추천 코스, 필수 정보 |
| `story` 스토리 | 줄거리, 등장인물, 명장면 |
| `tips` 팁/노하우 | 팁 목록, 단계별 방법 |
| `report` 보고서 | 목차, 섹션별 요약, 결론 |

### 1-3. 다른 카테고리로 재분석
- 결과 페이지 "🔄 다시 분석" 탭에서 카테고리 직접 선택
- 기존 자막 캐시 재활용 (API 재호출 없음)

### 1-4. 언어 설정
- 영어 자막 영상: 한글 요약 / 원문(영어) 요약 선택
- 전체자막 탭: 한글 번역본 / 영문 원본 토글 (번역된 경우만 표시)

### 1-5. 웹 페이지 분석
- YouTube 외 일반 URL 분석 (뉴스, 블로그 등)
- 페이지 텍스트 크롤링 → 카테고리 분류 → 요약

### 1-6. PDF 분석
- PDF 업로드 → 텍스트 추출 → AI 요약
- 페이지 번호 타임스탬프 표시 (p.N 형식)

### 1-7. 음성 메모 분석
- 마이크 녹음 → Gemini STT → 내용 요약

---

## 2. 결과 페이지 (`/result/[sessionId]`)

### 2-1. 탭 구조
- **기본 요약**: 카테고리별 요약 카드
- **전체 자막**: 타임스탬프별 자막 표시, 클립보드 복사 버튼, 한글/English 토글
- **🗂 구간 분석**: 30분+ 영상 전용, 구간별 헤드라인 + 퀴즈
- **🔄 다시 분석**: 다른 카테고리로 재요약

### 2-2. 타임라인 바
- 요약 내 타임스탬프를 영상 전체 길이 대비 시각화
- 클릭 시 해당 구간으로 영상 점프 + 기본 요약 탭 자동 전환

### 2-3. 유튜브 임베드 플레이어
- 타임스탬프 뱃지 클릭 → 해당 구간 자동 재생
- 수업 모드(`?classView=1`)에서 자동 로그 기록

### 2-4. 퀴즈
- Gemini로 자동 생성 (4지선다 + 플래시카드 혼합)
- 오답 시 힌트 제공, 정답률 표시
- 재도전 버튼
- 오답 → `review_schedules` 복습 스케줄 자동 등록

### 2-5. 저장 기능
- 폴더 선택 모달 → Firestore `saved_summaries`에 저장
- 공개/비공개 설정 (스퀘어 노출 여부)
- AI 폴더 자동분류 제안

### 2-6. 북마크
- 타임스탬프 + 메모 저장
- 마이페이지 🔖 북마크 탭에서 전체 관리

### 2-7. 댓글
- 플랫폼 자체 댓글 작성 (세션 단위)
- AI 자동 댓글 생성 버튼
- 유튜브 원본 댓글 요약 (인기/최신 분리)

### 2-8. 블로그 초안 생성
- SEO 최적화 제목, 메타 디스크립션, 슬러그
- 섹션별 본문, FAQ, 체크리스트 자동 생성
- HTML / 일반 텍스트 복사 (티스토리·워드프레스·네이버 호환)
- 목차 클릭 → 해당 섹션 스크롤 이동

### 2-9. 쇼츠 스크립트
- 유튜브 쇼츠 / 릴스용 짧은 스크립트 자동 생성
- 마이페이지 🎬 탭에 저장

### 2-10. 영어 워크시트 (영어 카테고리 전용)
- 레벨 선택 (초급/중급/고급)
- 어휘 목록 + 문제 세트 생성
- 저장 버튼 → 마이페이지 📋 워크시트 탭에 저장

### 2-11. 수업 모드 (`?classView=1`)
- 학생이 배포된 영상 시청 시 자동 활성화
- 저장 버튼 숨김, 이탈 확인 팝업 없음
- 시청 기록 자동 로깅 (play, meta, quiz, comment)

### 2-12. 자기점검 메타 반응 (수업 모드)
- ✅ 완전이해 / ⚠️ 혼동 / ❌ 미이해 버튼
- 교사 대시보드에 즉시 집계

### 2-13. AI 도슨트 채팅
- 요약 내용 기반 Q&A 챗봇
- 하단 플로팅 채팅 버튼

### 2-14. 보고서 PDF
- 요약 + 타임라인 + 퀴즈 결과 → PDF 다운로드

### 2-15. 공유
- 공유 링크 생성 (비로그인 접근 가능)
- 공개 설정된 요약은 스퀘어에 자동 노출

---

## 3. 마이페이지 (`/mypage`)

### 3-1. 폴더 관리
- 트리 구조 폴더 생성/삭제/이름변경
- 드래그앤드롭 이동
- 하위폴더 무제한 생성
- 폴더별 공유 링크 생성
- AI 폴더 자동분류 (새 요약 저장 시 추천)

### 3-2. 저장된 요약
- 카테고리별 필터링
- 목록 / 그리드 뷰
- 클릭 → 결과 페이지 이동

### 3-3. 블로그 초안 탭 (📝)
- 저장된 블로그 초안 목록 / 열기·삭제

### 3-4. 쇼츠 스크립트 탭 (🎬)
- 저장된 쇼츠 스크립트 목록

### 3-5. 여행 위시리스트 탭 (🧳)
- 여행지 위시리스트 (장소 추가/삭제)
- Google Maps 연동 장소 검색

### 3-6. 여행 일정 탭 (🗺)
- AI 생성 여행 일정 저장
- 일정 마법사 모달 (목적지, 기간, 예산 입력)
- 여행 다이어리 작성

### 3-7. 북마크 탭 (🔖)
- 영상별 타임스탬프 북마크 + 메모 전체 목록

### 3-8. 비디오 퀴즈 탭 (🎯)
- 저장된 비디오 퀴즈 세트 목록
- 퀴즈 관리 모달

### 3-9. 워크시트 탭 (📋)
- 저장된 영어 워크시트 목록
- 썸네일, 제목, 레벨 뱃지, 단어수/문제수 표시
- 열기 → WorksheetPanel, 삭제

### 3-10. 유튜브 임포트 탭
- 유튜브 채널/재생목록 일괄 임포트

### 3-11. 친구 요청 관리
- 친구 추가/수락/거절
- 1:1 메시지(DM) 연결

---

## 4. 스퀘어 (`/square`)

- 공개 설정된 요약 카드 갤러리 (최대 300개)
- 매거진 포스트 탭 병행
- 카테고리 필터 + 텍스트 검색
- 자동수집 뱃지 표시, 조회수 표시
- 어드민: hover 시 숨김/삭제 버튼 노출

---

## 5. AI 매거진 (`/magazine`)

### 5-1. 매거진 목록
- AI 자동생성 큐레이션 기사 목록
- 토픽 클러스터: AI 소식 / AI 도구 / AI 활용
- Featured 카드(대형) + 그리드 카드
- 발행일, 읽기시간, 조회수, 태그 표시

### 5-2. 매거진 상세 페이지
- 마크다운 본문 (Gemini SEO 최적화 생성)
- **목차(TOC)**: 클릭 → 해당 섹션 부드럽게 스크롤
- 핵심 요약 블록 (executive summary)
- FAQ 아코디언
- 심층 분석 (배경 맥락, 핵심 개념 강의노트, 단계별 가이드)
- 유튜브 시청자 반응 요약 (인기/최신 댓글)
- 관련 포스트 3개
- 댓글 (작성/좋아요)
- 하단 CTA (AI 요약하기 / 매거진 전체보기)

### 5-3. 자동생성 크론
- 매일 3회 자동 발행 (KST 오전 6시 / 오후 2시 / 오후 10시)
- 핫 점수 높은 요약 선별 → Gemini 기사 생성 → Firestore 저장

### 5-4. 토픽별 페이지
- `/magazine/topic/[cluster]` — 토픽별 전체 목록

---

## 6. 크롬 확장프로그램

- YouTube 영상 페이지 제목 아래 **"쏙튜브 요약저장"** 버튼 자동 주입
- 클릭 → `/api/summarize` + `/api/extension/save` 호출 (백그라운드)
- 진행 상태 표시: 요약 중(스피너) → 완료(초록 "확인하기" 버튼) / 오류
- 1분 경과 시 "자막 분석 중..." 메시지 전환
- 3분 초과 시 타임아웃 처리 + ssoktube.com 직접 열기 링크 제공
- Firebase IndexedDB에서 인증 토큰 자동 읽기 / 만료 시 자동 갱신
- YouTube SPA 네비게이션 감지 → 영상 전환 시 버튼 자동 재주입
- 미로그인 시 "ssoktube.com에 먼저 로그인해 주세요" 안내

---

## 7. 검색

- 자연어 임베딩 기반 시맨틱 검색 (`text-embedding-005`)
- 저장된 요약 전체 대상
- 카테고리 필터 병행

---

## 8. 메시지 & 소셜

- 1:1 DM (MessagesModal)
- 친구 요청/수락/거절
- 읽지 않은 메시지 뱃지 (헤더 30초 주기 폴링)
- 프로필 페이지 (`/profile/[userId]`): 공개 요약 목록

---

## 9. 학습 그룹 방 (`/room/[roomId]`)

- 멤버 초대형 학습 그룹
- 공유 노트 및 그룹 토론

---

## 10. 인증 & 계정

- Firebase Auth (이메일/소셜 로그인)
- 역할 구분: 일반 / 선생님 / 학생
- 요금제: 무료 / 유료 (현재 PRICING_ENABLED=false — 전원 무제한)
- 프로필 설정 (이름, 역할, 학교, 클래스)
- 아바타 업로드 (Firebase Storage)
- 계정 삭제

---

## 11. 어드민 (`/admin`)

- **통계 탭**: 오늘 분석 수, 전체 분석, 저장 수, 누적 가입자
- **영상 관리 탭**: 전체 요약 검색·삭제
- **회원 관리 탭**: 역할/플랜별 필터
- **스퀘어 관리 탭**: 공개 카드 숨김/삭제
- **매거진 탭**: URL 또는 sessionId 입력 → 즉시 기사 생성
- **분석 탭**: GA4 외부 대시보드 링크

---

## 12. SEO & 공유

- 결과 페이지 OG 이미지 자동 생성 (`opengraph-image`)
- JSON-LD 스키마 (Article, VideoObject, FAQPage)
- 매거진 포스트 메타태그 완전 최적화
- 공유 링크 비로그인 접근 허용

---

## 13. 광고

- 카테고리 맥락 기반 광고 배너 (상단/하단)
- 일반 정적 광고 배너

---

## 14. 기술 스택

| 영역 | 기술 |
|------|------|
| 프레임워크 | Next.js 14 App Router, TypeScript |
| 인증/DB | Firebase Auth, Firestore, Storage |
| AI | Gemini 2.5 Flash (요약/분류/생성), Claude API |
| 자막 추출 | SocialKit API → Gemini STT 폴백 |
| 임베딩 검색 | Google `text-embedding-005` |
| 배포 | Vercel (크론 포함) |
| 확장 | Chrome Extension MV3 |
