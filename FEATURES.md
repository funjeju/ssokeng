# 쏙튜브 기능 명세서

> 이 문서는 쏙튜브(nextcurator)에 구현된 모든 기능을 총망라한 공식 명세서입니다.  
> 새 기능이 추가될 때마다 하단 [릴리즈 로그](#릴리즈-로그)에 기록합니다.

---

## 목차

1. [서비스 개요](#1-서비스-개요)
2. [콘텐츠 입력 및 AI 분석](#2-콘텐츠-입력-및-ai-분석)
3. [결과 페이지 (Result)](#3-결과-페이지-result)
4. [마이페이지 / 개인 라이브러리](#4-마이페이지--개인-라이브러리)
5. [스퀘어 (Square) — 커뮤니티 피드](#5-스퀘어-square--커뮤니티-피드)
6. [클래스룸 — 교사·학생 학습 관리](#6-클래스룸--교사학생-학습-관리)
6a. [매거진 (Square K)](#6-a-매거진-square-k)
7. [콘텐츠 생성 도구](#7-콘텐츠-생성-도구)
8. [검색 및 추천](#8-검색-및-추천)
9. [소셜 기능](#9-소셜-기능)
10. [어드민 대시보드](#10-어드민-대시보드)
11. [사용자 역할 및 권한](#11-사용자-역할-및-권한)
12. [인증 및 보안](#12-인증-및-보안)
13. [API 엔드포인트 목록](#13-api-엔드포인트-목록)
14. [릴리즈 로그](#릴리즈-로그)

---

## 1. 서비스 개요

**쏙튜브**는 유튜브 영상·음성 녹음·PDF 문서를 AI로 분석해 콘텐츠 유형에 맞는 구조화된 요약을 자동 생성하고, 학습·지식 관리·협업 교육까지 연결하는 AI 지식 큐레이션 플랫폼입니다.

| 항목 | 내용 |
|---|---|
| 기술 스택 | Next.js (App Router), Firebase, Gemini 2.5 Flash, Claude (Anthropic), TypeScript, Tailwind CSS |
| 기본 테마 | 다크 모드 (라이트 전환 가능) |
| 지원 환경 | 모바일 / 태블릿 / 데스크톱 (반응형) |
| AI 엔진 | Google Gemini 2.5 Flash (전사·분류·요약), Claude (Anthropic) (범주별 심층 요약), text-embedding-004 (벡터 검색) |

---

## 2. 콘텐츠 입력 및 AI 분석

### 2-1. 유튜브 URL 분석

- 홈 화면 URL 입력창에 유튜브 주소 붙여넣기
- 영상 ID 추출 → 메타데이터(제목·채널·길이·게시일) 자동 획득
- 자막(자동 생성 포함) 전문 추출
- Gemini로 **9개 카테고리 자동 분류**:

  | 카테고리 | 아이콘 | 설명 |
  |---|---|---|
  | recipe | 🍳 | 요리·레시피 영상 |
  | english | 🔤 | 영어 학습 |
  | learning | 📐 | 학습·강의 |
  | news | 🗞️ | 뉴스·시사 |
  | selfdev | 💪 | 자기계발 |
  | travel | 🧳 | 여행 |
  | story | 🍿 | 스토리·영화·드라마 |
  | tips | 💡 | 팁·노하우 |
  | report | 📋 | 보고서·리포트 |

- **캐시 시스템**: 동일 videoId 재분석 시 저장된 결과 즉시 반환 (API 비용 절감)
- 분석 결과 Firestore `summaries` 컬렉션에 자동 저장

### 2-2. 음성 녹음 분석

- 홈 화면에서 **즉시 녹음** 또는 **오디오 파일 업로드** (무료 회원 최대 20MB / 어드민 무제한 / TODO: 유료Pro 100MB)
- 지원 형식: webm, mp3, m4a, ogg 등 audio/* MIME 타입 전체
- Gemini 멀티모달로 음성 전사(STT) + 제목 자동 생성
- 전사 텍스트를 Claude 파이프라인으로 넘겨 **10개 카테고리 자동 분류 + 요약**
- 분류된 카테고리 색상의 **SVG 썸네일 자동 생성** (🎙 아이콘 포함)
- 분석 완료 후 로그인 사용자의 마이페이지에 **비공개로 자동 저장**

### 2-3. PDF 문서 분석

- 홈 화면에서 PDF 파일 업로드 (무료 회원 최대 20MB / 어드민 무제한 / TODO: 유료Pro 100MB)
- Gemini로 PDF 전체 텍스트 추출 (텍스트 기반 + 이미지/스캔 PDF OCR 모두 처리)
- Claude 파이프라인으로 카테고리 분류 + 요약 + 리포트 요약 생성
- 분류된 카테고리 색상의 **SVG 썸네일 자동 생성** (📄 아이콘 포함)
- 분석 완료 후 로그인 사용자의 마이페이지에 **비공개로 자동 저장**

### 2-4. 카테고리 재분석 (Re-analyze)

- 결과 페이지에서 다른 카테고리로 즉시 전환 분석
- 유튜브 영상: `/api/summarize` 재호출
- 음성·PDF: `/api/reanalyze-transcript` — 원본 전사 텍스트 재활용 (재전사 없음)
- 새 sessionId 발급 + Firestore 저장

---

## 3. 결과 페이지 (Result)

### 3-1. 영상 플레이어

- YouTube IFrame Player 임베드
- 자동재생·일시정지·시간이동 지원
- 타임스탬프 클릭 시 해당 시점으로 점프

### 3-2. 카테고리별 AI 요약 레이아웃

각 카테고리마다 **특화된 구조**로 요약을 표시합니다:

| 카테고리 | 표시 항목 |
|---|---|
| **recipe** | 완성 이미지, 재료 목록, 단계별 타임스탬프 + 설명, 난이도, 조리 시간 |
| **english** | 핵심 표현, 어휘, 문법 패턴, 문화 맥락 |
| **learning** | 핵심 개념, 주요 포인트, 예시 + 타임스탬프 |
| **news** | 헤드라인, 3줄 요약, 5W1H 분석, 시사점 |
| **selfdev** | 핵심 메시지, 인사이트, 실천 체크리스트, 명언 |
| **travel** | 여행지, 장소 목록, 동선, 실용 정보, 주의사항 |
| **story** | 등장인물, 타임라인, 장르, 결말 분석 |
| **tips** | 번호 매긴 팁 목록, 도구·준비물, Top 3 하이라이트 |
| **report** | 목차, 섹션별 정리, 결론 |

### 3-3. 도슨트 채팅 (Docent Chat)

- 결과 페이지 우하단 플로팅 버튼으로 상시 접근
- 현재 요약 콘텐츠를 컨텍스트로 멀티턴 대화
- "이 영상에서 가장 중요한 부분이 뭐야?", "3단계를 더 쉽게 설명해줘" 등 자유 질문

### 3-4. 세그먼트 댓글 (Segment Comment)

- 요약 내 특정 단락·단계에 말풍선 형태로 댓글 등록
- 등록된 세그먼트 댓글은 **하단 전체 댓글 목록에도 자동 반영** (📌 배지 포함)
- 댓글 목록에서 단락명 클릭 시 해당 세그먼트로 스크롤

### 3-5. 전체 댓글 섹션

- 루트 댓글 + 대댓글 (threaded)
- 학생 댓글 제출 시 교사 대시보드에 **활동 로그 자동 기록**
- 댓글 수 표시

### 3-6. 시청자 반응 요약

- 결과 페이지 CommentSection 위에 표시되는 lazy-load 카드
- SocialKit으로 인기 댓글 30개 수집 → Gemini 280자 요약 (긍정/부정/혼재 방향성 + 주요 언급 포인트)
- 분석 요청 시 병렬로 수집·저장 → 이후 조회 시 캐시 반환 (추가 API 호출 없음)

### 3-7. 저장 및 공유

- **라이브러리에 저장**: 폴더 선택 + 공개 여부(비공개/공개) 설정 후 저장
- **스퀘어에 공개 발행**: 커뮤니티 피드에 등록
- **링크 복사**: 결과 페이지 URL 클립보드 복사
- **SNS 공유**: OG 태그 자동 설정으로 카카오·트위터 등 미리보기 지원

### 3-8. 번역

- 요약 내 특정 섹션을 원하는 언어로 번역
- 번역 버튼 클릭 → 인라인 번역 표시

### 3-9. 음성 컨트롤 (요리 영상 전용)

요리 영상 결과 페이지 하단 좌측 고정 마이크 버튼:

- **마이크 클릭 한 번** → 페이지 나갈 때까지 계속 켜짐 (자동 연속 인식)
- **명령 실행 후 자동 재시작** — 매번 탭할 필요 없음
- 에코 캔슬링(`echoCancellation`, `noiseSuppression`) 명시 적용
- 영상이 스피커로 재생 중이어도 오인식 최소화

**지원 음성 명령:**

| 명령 예시 | 동작 |
|---|---|
| "3단계", "세 단계로 이동" | 해당 단계 타임스탬프로 이동 |
| "10초 뒤로", "30초 앞으로" | 현재 위치에서 N초 이동 |
| "처음부터", "처음으로" | 영상 처음으로 이동 후 재생 |
| "재생", "틀어", "계속" | 영상 재생 |
| "멈춰", "일시정지", "스톱" | 영상 일시정지 |
| "빠르게", "빨리" | 배속 +0.25 |
| "느리게", "천천히" | 배속 -0.25 |
| "1.5배속", "2배속" | 해당 배속으로 직접 지정 |
| "보통", "정상 속도" | 1.0배속으로 복원 |

---

## 4. 마이페이지 / 개인 라이브러리

### 4-1. 프로필 카드

- 아바타 (이모지 선택 또는 사진 업로드)
- 이름, 이메일, 역할 배지 (일반·교사·학생)
- 토큰 잔액 표시
- 프로필 완성 유도 인센티브
- 교사로 전환 / 회원 탈퇴

### 4-2. 폴더 관리

- **생성·이름 변경·삭제**
- **3단계 공개 설정**: 비공개 / 친구 공개 / 전체 공개
- **공유 링크 생성**: 폴더별 고유 shareId → URL 복사
- 폴더 내 요약 **드래그 앤 드롭 재정렬**

### 4-3. 저장된 요약 관리

- 마순리 그리드 레이아웃
- **카테고리 필터** (전체·recipe·english·... 10개)
- **NLP 전문 검색**: 키워드 + 벡터 유사도 하이브리드
- 요약 이동 (다른 폴더로)
- 요약 삭제
- 태그·폴더 정보 표시

### 4-4. 서브 탭

| 탭 | 내용 |
|---|---|
| 라이브러리 | 폴더별 저장 요약 전체 |
| 친구 | 친구 요청·수락·거절·목록 |
| 여행 | 저장된 여행지(위시리스트) + AI 생성 일정 |
| 블로그 초안 | SEO 블로그 아티클 목록 |
| 숏츠 스크립트 | 추출된 숏폼 클립 목록 |
| 북마크 | 타임스탬프 메모 목록 |
| 퀴즈 | 저장된 세그먼트 퀴즈 목록 |
| YouTube 가져오기 | 유튜브 재생목록 영상을 선택해 AI 일괄 요약 |
| 수업자료 *(학생 전용)* | 선생님이 배포한 폴더·영상 실시간 열람 |

---

## 5. 스퀘어 (Square) — 커뮤니티 피드

### 5-1. 피드 표시

- 마순리 그리드 (데스크톱 4열 / 태블릿 3열 / 모바일 2열)
- 카드마다: 썸네일, 카테고리 배지, 제목, 채널명, 좋아요·조회수
- 카드 13개마다 Google AdSense 광고 슬롯 삽입
- AI 기반 추천 카드 (사용자 좋아요 카테고리 기반, 없으면 인기 콘텐츠)
- **자동 수집 크론**: 매일 KST 04:00 실행. YouTube Data API로 뉴스·자기계발·여행·팁·영어·요리 6개 카테고리 핫한 KR 영상 자동 수집 → 자막 추출·요약·저장 (`autoCollected: true`)
  - 품질 필터: 3분 미만 영상 제외, 고유 문장 비율 < 20% (반복 자막) 폐기
- **어드민 스퀘어 관리**: hover 시 숨김(노란)/삭제(빨간) 버튼 표시 → 어드민 대시보드 스퀘어 탭에서 검색·페이지네이션·숨김 영상 포함 보기

### 5-2. 필터 및 정렬

- **카테고리 필터**: 전체·recipe·english·learning·news·selfdev·travel·story·tips
- **정렬**: 최신순 / 인기순(좋아요) / 조회수순
- **자연어 검색** (NLP)

### 5-3. 카드 액션

- 좋아요 / 좋아요 취소 (카운트 실시간 반영)
- 조회수 자동 집계
- 작성자에게 메시지 보내기 (본인 제외)
- 본인 발행 요약 삭제

---

## 6. 클래스룸 — 교사·학생 학습 관리

### 6-1. 클래스 생성 (교사)

- 학교명·학년·반 입력 → **6자리 고유 클래스 코드** 자동 발급
- 마스터 폴더 자동 생성 (교사 라이브러리와 연동)
- 코드 공유로 학생 초대

### 6-2. 학생 등록

- 클래스 코드 + 이름으로 가입
- 전용 이메일 자동 발급: `{classCode}.{nameBase64}@cls.ssoktube.com`
- 가입 시 Firebase Auth custom claim (`classCode`, `role: student`) 자동 설정
- 가입 시 교사 마스터 폴더 영상을 **서버사이드(Admin SDK)**로 복제 (Firestore 보안 규칙 우회)

### 6-3. 교사 대시보드

- **전체 통계**: 총 학생 수, 활동량, 퀴즈 성취도
- **학생별 상세 로그**:
  - 로그인 / 로그아웃 이력
  - 영상 시청 (시작·완료·시청 시간·진행률)
  - 퀴즈 시도 횟수·정답률
  - 이해도 마커: 완료(✅) / 헷갈림(🤔) / 모름(❓) 횟수
  - 댓글 제출 이력
- **영상별 분석**: 어떤 영상을 누가 얼마나 봤는지 집계
- **수업자료 배포/회수 시스템** (폴더 단위):
  - `배포` 버튼 → 폴더에 `distributedClassCodes` 추가, 학생 화면에 즉시 반영
  - `회수` 버튼 → 즉시 학생 화면에서 제거
  - 영상 복사 없음 — 학생이 교사의 원본을 실시간 읽기 (Live View)
  - 배포 후 교사가 추가한 영상도 자동 반영
- **클립 배포**: 폴더 내 특정 영상의 구간(시작~종료 초)만 학생에게 배포
- **퀴즈 오답 히트맵** (`🔥 퀴즈 히트맵` 탭): 영상별·문제별 오답률 색상 시각화
- **수업 보고서**: 폴더별 학생 참여도 집계 + 교사 코멘트 + PDF 다운로드
- **에빙하우스 복습 현황**: 학생 상세 모달에서 오답 복습 일정 추적

### 6-4. 학생 경험

- 마이페이지 **수업자료 탭** (`📖 수업자료`): 선생님이 배포한 폴더·영상 실시간 열람
  - 별도 복사 없이 원본 폴더에서 읽음 → 교사가 추가한 신규 영상도 즉시 보임
  - 회수된 폴더는 즉시 목록에서 사라짐
  - 학생은 수업자료에 대한 쓰기 권한 없음 (완전 읽기 전용)
- 이해도 마커 클릭 (완료·헷갈림·모름)
- 퀴즈 응시
- 세그먼트·전체 댓글 작성 (교사 대시보드에 자동 기록)
- 개인 라이브러리 별도 유지
- 오답 발생 시 에빙하우스 복습 일정 자동 등록

### 6-5. 활동 자동 로깅

`/api/classroom/activity` 엔드포인트로 다음 이벤트 자동 기록:

| 이벤트 타입 | 기록 내용 |
|---|---|
| `login` | 접속 시각 |
| `logout` | 로그아웃 시각 |
| `watch_start` | 영상 시청 시작 |
| `watch_complete` | 시청 완료 |
| `watch_progress` | 시청 진행률(%) |
| `quiz` | 정답 여부, 점수 |
| `meta` | 이해도 마커 레벨 |
| `comment` | 댓글 텍스트 |

---

## 6-a. 매거진 (Square K)

> AI 큐레이션 요약을 SEO 최적화 롱폼 아티클로 자동 변환하는 콘텐츠 발행 시스템

- **자동 생성**: hotScore 알고리즘으로 최적 요약 선택 → 매거진 포스트 자동 생성
- **수동 생성**: 어드민 큐레이션 탭에서 특정 요약 URL 입력 → 초안 또는 즉시 발행
- **포스트 구성**: 제목, 메타 설명, 본문(마크다운), FAQ, 핵심 체크리스트, 유튜브 댓글 요약, 플랫폼 학습자 반응
- **관련 포스트**: 같은 카테고리 우선 3개 자동 연결
- **SEO 최적화**: H2/H3 키워드 배치, featured snippet 구조, People Also Ask FAQ 형식
- **스퀘어 탭 노출**: Square K 피드에서 매거진 탭으로 분리 표시

---

## 7. 콘텐츠 생성 도구

### 7-1. 세그먼트 퀴즈

- english·learning 카테고리 결과 페이지에서 활성화
- 플래시카드 + 객관식 문제 자동 생성
- 학생 응시 시 교사 대시보드에 결과 자동 기록

### 7-2. 영어 워크시트

- 영어 영상 결과 페이지에서 생성
- **3가지 수준**: 초급(CEFR A1-A2) / 중급(B1) / 고급(B2+)
- 항목: 어휘 목록, 매칭 문제, 빈칸 채우기, 번역 문제
- PDF 다운로드 가능

### 7-3. 블로그 초안 생성

- 요약을 SEO 최적화 블로그 아티클로 자동 변환
- 항목: 제목, 도입부, 본문 섹션, 결론, SEO 키워드
- Markdown 형식 저장 → 마이페이지 "블로그 초안" 탭에서 관리

### 7-4. 숏츠 스크립트 추출

- 자막 전문 분석으로 **3~5개 숏폼 클립 구간 추천**
- 각 클립: 시작/종료 시각, 스크립트, 제목, 훅 이유, 유형(hook/tip/highlight/emotion), 초 단위 길이
- 전체 영상 편집 팁 포함
- 마이페이지 "숏츠 스크립트" 탭에서 관리

### 7-5. 여행 일정 생성 (AI 플래너)

- 마이페이지 여행 위시리스트의 저장 장소를 기반으로 AI 일정 생성
- **모드 선택**: 저장 장소만 / AI 추천 장소 포함
- 입력: 지역, 날짜 범위, 숙소, 도착 시각
- 출력: 시간대별 세부 일정 + 이동 팁
- 마이페이지 "여행" 탭에서 관리

### 7-6. 멀티 리포트 (다중 영상 분석)

- 1~5개 저장 요약 선택 + 리포트 제목·목적 입력
- AI가 영상 간 교차 분석 생성:
  - 종합 요약, 핵심 테마, 인사이트(출처 영상 인용), 공통점·차이점, 우선순위별 액션 아이템, 결론
- PDF 다운로드

---

## 8. 검색 및 추천

### 8-1. 마이페이지 검색

- 자연어 질의 처리 (NLP 키워드 분석)
- 벡터 유사도 검색 (text-embedding-004, 768차원)
- 키워드 매칭 + 벡터 검색 **하이브리드**
- 카테고리 필터 병행 적용

### 8-2. 스퀘어 검색

- 공개 요약 전체 대상 자연어 검색
- 카테고리·정렬 필터와 조합

### 8-3. 개인화 추천

- 사용자의 좋아요 이력에서 카테고리 가중치 계산
- 가중치 기반으로 스퀘어 피드 상단에 추천 카드 삽입
- 좋아요 이력 없으면 인기 콘텐츠로 대체

### 8-4. 도슨트 채팅 검색 (RAG)

- `/api/chat`: 사용자 질의 임베딩 → 저장 요약 코사인 유사도 검색 → 상위 3개 컨텍스트로 응답 생성
- 저장된 지식베이스 전체를 자연어로 질의 가능

---

## 9. 소셜 기능

### 9-1. 친구 시스템

- 친구 요청 보내기 / 수락·거절·취소
- 친구 목록 관리
- 친구 공개 폴더 열람 가능

### 9-2. 프로필 페이지

- 공개 요약 마순리 그리드 표시
- 공개/친구 공개 폴더 목록
- 폴더 클론 (출처 표기 포함하여 내 라이브러리에 복사)
- 친구 관리 버튼 (요청·수락·제거)
- 메시지 바로가기

### 9-3. 1:1 메시지

- 대화 목록 및 메시지 내역
- 스퀘어 카드에서 작성자에게 바로 메시지 보내기
- 읽음 상태 표시

### 9-4. 폴더 공유

- 폴더별 공개 설정 (비공개 / 친구 공개 / 전체 공개)
- 고유 공유 링크 → 로그인 없이 열람 가능
- 공유 페이지에서 폴더 내 요약 보기 + 내 라이브러리로 복사

---

## 10. 어드민 대시보드

접근: 환경 변수로 지정된 어드민 이메일만 접근 가능

### 10-1. 통계 탭

- 일/주/월별 분석 요약 건수
- 사용자 증가 추이
- 카테고리 분포
- 인기 영상·채널

### 10-2. 영상 관리 탭

- 분석된 전체 영상 목록 (페이지네이션)
- 제목·사용자·videoId 검색
- 썸네일 미리보기
- 캐시 여부 확인
- 영상 영구 삭제

### 10-3. 사용자 관리 탭

- 전체 사용자 목록 + 검색 (이름·이메일·학교·클래스 코드)
- 역할 필터 (교사 / 학생 / 일반 / 전체)
- 플랜 필터 (무료 / 유료)
- 사용자별: 역할·플랜·클래스 소속·토큰 수 조회

### 10-4. 스퀘어 관리 탭

- 공개 피드 전체 목록 + 검색·페이지네이션
- 숨김 영상 포함 보기 토글
- 카드 hover 시 숨김(노란)/삭제(빨간) 버튼 → 불량 콘텐츠 즉시 처리

### 10-5. 매거진 큐레이션 탭

- 매거진 포스트 목록 + 수동 생성 트리거
- 특정 요약 URL 입력 → 즉시 매거진 생성 (초안/즉시발행 선택)
- GA4 외부 링크 배너 (트래픽 통계 바로가기)

---

## 11. 사용자 역할 및 권한

### 일반 사용자

- 유튜브 URL·음성·PDF 분석 무제한
- 개인 라이브러리·폴더 관리
- 스퀘어 공개 발행, 좋아요·댓글
- 친구 추가·메시지
- 블로그·숏츠·워크시트·리포트·일정 생성
- 도슨트 채팅, 세그먼트 댓글
- 교사 또는 학생으로 역할 전환 가능

### 교사

- 위 일반 기능 전부 포함
- 클래스 생성 (고유 코드 발급)
- 학생 초대 및 관리
- 교사 전용 대시보드: 학생 활동 로그·통계 열람
- 영상 배포 (학생 라이브러리에 푸시)
- 마스터 폴더 관리

### 학생

- 클래스 코드로 가입 (`@cls.ssoktube.com` 전용 계정)
- 교사 배정 영상 열람
- 이해도 마커, 퀴즈 응시, 댓글 작성
- 활동이 교사 대시보드에 자동 기록
- 개인 라이브러리 별도 유지

### 어드민

- 위 모든 기능 포함
- 플랫폼 전체 통계·관리 대시보드
- 사용자·영상 삭제 (모더레이션)

---

## 12. 인증 및 보안

### 인증 방법

- Google OAuth (소셜 로그인)
- 이메일·비밀번호 (이메일 인증 포함)
- 비밀번호 재설정 이메일
- 학생 전용 클래스룸 로그인 (`@cls.ssoktube.com`)

### 보안

- Firestore 보안 규칙 (userId 기반 문서 접근 제어)
- 역할 기반 접근 제어 (교사·학생·어드민)
- 어드민 이메일 게이팅 (환경 변수)
- 계정 삭제 시 연관 데이터 캐스케이드 삭제
- 개인 요약·폴더 기본 비공개

---

## 13. API 엔드포인트 목록

### 분석 · 요약

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/summarize` | 유튜브 영상 AI 분석 (캐시 포함) |
| POST | `/api/summarize-voice` | 음성 녹음·파일 전사 + 요약 |
| POST | `/api/summarize-pdf` | PDF 문서 OCR + 요약 |
| POST | `/api/reanalyze-transcript` | 음성·PDF 다른 카테고리로 재분석 |

### 콘텐츠 생성

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/quiz` | 퀴즈 생성 (english·learning) |
| POST | `/api/segment-quiz` | 세그먼트별 퀴즈 생성 |
| POST | `/api/worksheet` | 영어 워크시트 생성 (3단계) |
| POST | `/api/blog-draft` | SEO 블로그 초안 생성 |
| POST | `/api/shorts-script` | 숏츠 클립 스크립트 추출 |
| POST | `/api/travel-itinerary` | AI 여행 일정 생성 |
| POST | `/api/multi-report` | 다중 영상 교차 분석 리포트 |

### 채팅 · 대화

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/chat` | 개인 라이브러리 RAG 검색·응답 |
| POST | `/api/docent-chat` | 결과 페이지 도슨트 멀티턴 대화 |
| POST | `/api/comment-analysis` | 댓글 감성·테마 분석 |

### 검색 · 유틸

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/search` | 하이브리드 검색 (키워드+벡터) |
| POST | `/api/translate` | 요약 섹션 번역 |
| POST | `/api/embed-query` | 텍스트 임베딩 벡터 반환 |
| POST | `/api/extract-items` | 요약에서 구조화 데이터 추출 |
| GET  | `/api/video-duration` | 영상 길이·메타데이터 조회 |
| GET  | `/api/video-published-at` | 영상 게시일 조회 |
| POST | `/api/proxy-image` | 이미지 프록시 |
| POST | `/api/card-image` | 공유용 카드 이미지 생성 |
| POST | `/api/folder-classify` | 요약 폴더 자동 분류 제안 |

### 클래스룸

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/classroom/create` | 클래스 생성 (교사) |
| POST | `/api/classroom/enroll` | 학생 등록 (custom claim 포함) |
| POST | `/api/classroom/activity` | 학생 활동 로그 기록 |
| POST | `/api/classroom/distribute` | 폴더 배포 (classCode → distributedClassCodes) |
| POST | `/api/classroom/recall` | 폴더 회수 (distributedClassCodes에서 제거) |
| GET  | `/api/classroom/distributed-folders` | 배포된 폴더 목록 조회 (학생 전용) |
| GET  | `/api/classroom/distributed-videos` | 배포된 폴더의 영상 목록 조회 (학생 전용) |

### 사용자·어드민

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/user/delete` | 계정 삭제 (캐스케이드) |
| POST | `/api/admin/stats` | 플랫폼 전체 통계 |
| POST | `/api/admin/summaries` | 전체 요약 목록 (검색·페이지네이션, userId 포함) |
| POST | `/api/admin/users` | 전체 사용자 목록 (필터) |
| POST | `/api/admin/analytics` | 상세 분석 차트 데이터 |
| POST | `/api/admin/delete` | 요약 영구 삭제 (어드민) |
| POST | `/api/admin/square` | 스퀘어 목록 조회 |
| PATCH | `/api/admin/square` | 스퀘어 카드 숨김/공개/삭제 |

### 매거진

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET/POST | `/api/cron/generate-post` | 매거진 포스트 생성 (자동/수동, sessionId 지정 가능) |
| GET  | `/api/cron/auto-collect` | 유튜브 자동 수집 크론 |
| GET  | `/api/magazine/comments` | 매거진 댓글 조회 |

### 기타 유틸

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/yt-comment-summary` | 유튜브 댓글 요약 (시청자 반응) |
| GET/POST/PATCH | `/api/review-schedule` | 에빙하우스 복습 일정 관리 |

---

## 릴리즈 로그

### v1.2 — 2026-04-30

**클래스룸 폴더 배포/회수 시스템 (Live View)**
- 기존 "기준폴더 지정 + 전체배포(복사)" 방식 폐기
- 교사 대시보드 수업자료 탭: 폴더별 **배포** / **회수** 버튼
- 배포: 폴더에 `distributedClassCodes` 배열에 classCode 추가 → 학생 즉시 열람 가능
- 회수: `distributedClassCodes`에서 제거 → 학생 화면에서 즉시 사라짐
- 영상 복사 없음 — 학생이 교사 원본 폴더를 서버 API 통해 실시간 읽기
- 교사가 나중에 추가한 영상도 별도 배포 없이 자동 반영
- 학생 마이페이지 `📖 수업자료` 탭 신설 (학생 계정에만 노출)
- `GET /api/classroom/distributed-folders`: 학생의 classCode로 배포된 폴더 목록
- `GET /api/classroom/distributed-videos`: 특정 폴더의 영상 목록 (권한 검증)
- 신규 가입 학생에게 Firebase Auth custom claim (`classCode`, `role`) 자동 설정
- 기존 학생도 모두 적용 — users 문서의 classCode 기반으로 동작

**어드민 패널 userId 수집**
- `/api/summarize` 요청 시 Authorization 헤더로 로그인 유저 인증 → summaries 문서에 `userId`, `userDisplayName` 저장
- 어드민 영상 관리 탭: "익명 (캐시)" 대신 실제 유저 이름 표시
- UrlInput에서 로그인 사용자는 자동으로 Authorization 헤더 포함

---

### v1.1 — 2026-04-18~20

**매거진 (Square K) 기능**
- 큐레이션된 AI 요약을 SEO 최적화 롱폼 아티클로 자동 변환
- 항목: 제목, 도입부, 본문 섹션, FAQ, 핵심 체크리스트, 시청자반응, 관련 포스트 3개
- 어드민 큐레이션 탭에서 수동 생성 (특정 요약 URL 지정 가능) 또는 hotScore 자동 선택
- 스퀘어 매거진 탭에서 발행 목록 표시
- 플랫폼 댓글(세그먼트 + 전체) 매거진 "SSOKTUBE 학습자 반응" 섹션에 반영

**유튜브 시청자 반응 요약**
- SocialKit으로 인기 댓글 30개 수집 → Gemini 280자 요약 (방향성 + 주요 포인트)
- 요약 분석 시 병렬로 수집·저장 → 이후 캐시 반환
- 결과 페이지 CommentSection 위에 lazy-load 카드로 표시

**스퀘어 자동 수집 크론**
- YouTube Data API로 6개 카테고리 KR 핫 영상 매일 KST 04:00 자동 수집
- 품질 필터: 3분 미만·반복 자막(고유 문장 비율 < 20%) 제외
- 댓글 요약까지 병렬로 생성

**어드민 스퀘어 관리 탭**
- 공개 피드 전체 목록, 숨김 영상 포함 보기
- 불량 콘텐츠 즉시 숨김/삭제

**로딩 UX 개선**
- 단계별 경과시간 표시, 30초 초과 시 안내메시지(5종 순환)
- 프로그레스바 animate-pulse, 총 경과시간 표시

**기타 버그 수정**
- AI 댓글 MAX_TOKENS 감지 → 경고 표시, maxOutputTokens 증가
- text-embedding-004 deprecated → text-embedding-005 변경
- 요약 API 병렬화 (getVideoInfo + getTranscript + getVideoMeta 동시 실행)

---

### v1.0 — 2026-04-16

**도슨트 채팅 — 현재 재생 위치 인식**
- `DocentChat`에 `playerRef`·`transcript` prop 추가
- 질문 전송 시 현재 재생 타임스탬프(M:SS) 자동 포함
- 타임스탬프 기반 ±45초 자막 구간 추출 → 도슨트 시스템 프롬프트에 주입
- "방금 나온 거 설명해줘"처럼 맥락 기반 질문에 정확하게 응답

**교사 — 특정 구간만 학생에게 배포 (클립 배포)**
- `pushVideoToClass`에 `clipStart`/`clipEnd` (초 단위) 파라미터 추가
- 교사 대시보드 수업자료 탭: 폴더 펼치면 영상 목록 표시 → 영상별 "🎬 구간 배포" 버튼
- 클립 설정 모달: 시작/종료 시간 입력(M:SS), 빈 칸이면 전체 영상 배포
- 학생 플레이어: `clipStart`에서 자동 시작, `clipEnd`에서 자동 정지 (200ms 폴링)

**퀴즈 오답 히트맵 (교사 대시보드 전체 보기)**
- `QuizAnswerLog`에 `question` 필드 추가 → 히트맵·복습에 문제 텍스트 활용
- `buildQuizHeatmap(logs)`: activity_logs → 영상별·문제별 오답률 집계
- `QuizHeatmap` 컴포넌트: 문제별 오답률 바 그래프, 색상 코딩 (녹색→노랑→주황→빨강)
- 교사 대시보드 "🔥 퀴즈 히트맵" 탭: 클래스 전체 영상별 취약 문제 한눈에 파악
- 오답 60% 이상 문제 "⚠ 집중 복습 필요" 자동 표시

**에빙하우스 망각곡선 복습 시스템**
- `/api/review-schedule` API: POST(오답 등록) / GET(오늘 복습 목록) / PATCH(복습 완료 처리)
- SM-2 유사 간격: 1→3→7→14→30→60일, 오답 시 한 단계 후퇴
- 학생 오답 발생 시 `review_schedule` Firestore 컬렉션에 자동 등록
- `ReviewBanner` 컴포넌트: 학생 로그인 시 오늘 복습 항목 플로팅 배너
  - 영상별 그룹핑, 클릭 시 해당 결과 페이지로 이동
- 교사 대시보드 학생 상세 모달 "🔁 복습 현황" 탭:
  - 학생별 복습 대기 항목 전체 목록
  - 🔴 오늘 이전(복습 필요) / 🔵 미래(예약됨) 구분
  - 문제 텍스트·복습 주기·다음 복습일 표시

---

### v0.9 — 2026-04-16

**음성·PDF 분석 파이프라인 전면 재설계**
- `summarize-voice`: Gemini 전사 → Claude 10개 카테고리 분류 + 요약 파이프라인으로 전환 (기존 단일 Gemini 호출 방식 교체)
- `summarize-pdf`: Gemini OCR + Claude 파이프라인 + 리포트 요약(`generateReportSummary`) 추가
- 음성·PDF 모두 카테고리 색상 **SVG 썸네일 자동 생성** (카테고리 아이콘 + 소스 아이콘)
- `reanalyze-transcript` API 신설: 원본 전사 텍스트 재활용 카테고리 재분석
- 분석 완료 후 로그인 사용자 마이페이지에 **비공개 자동 저장**
- 결과 페이지에서 sourceType=voice/pdf 감지 후 재분석 경로 분기

**세그먼트 댓글 → 전체 댓글 목록 연동**
- `SegmentCommentPopup` 댓글 등록 시 `window.CustomEvent('segment-comment-added')` 발행
- `CommentSection`에 이벤트 리스너 추가 → 세그먼트 댓글이 하단 목록에 즉시 반영
- 단락명 클릭 시 해당 세그먼트로 스크롤 (📌 배지)

**클래스룸 활동 로그 버그 수정**
- `log.value.level` → `log.value.metaLevel` 필드명 오타 수정 (classroom.ts, classroom/page.tsx 두 곳)
- 학생 로그아웃 시 `logout` 이벤트 자동 기록 (AuthProvider.signOut)
- 학생 댓글 제출 시 `comment` 이벤트 자동 기록 (ResultClient)

**음성 컨트롤 개선**
- 마이크 한 번 클릭 → 페이지 나갈 때까지 자동 연속 인식 (keep-on 기본화)
- "연속 듣기" 별도 토글 버튼 제거
- 명령어 추가: N초 뒤로/앞으로, 처음부터, N배속 직접 지정
- `getUserMedia` 에코 캔슬링 명시 (`echoCancellation`, `noiseSuppression`, `autoGainControl`)
